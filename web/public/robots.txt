# robots.txt for capture2calendar.app

# Allow all search engines to crawl everything
User-agent: *
Allow: /

# Sitemap location
Sitemap: https://capture2calendar.app/sitemap.xml

# Crawl-delay (optional, uncomment if needed)
# Crawl-delay: 1
